# Claude Code instructions

This project's working agreement is agent-agnostic and lives in `AGENTS.md`. Read it first.

@AGENTS.md

Global doctrine (response style, decide-don't-interrogate, phase discipline) comes from
`~/.claude/CLAUDE.md`. Add only project-specific rules below.

## Invariants

<copied from docs/idea-contract.md once Phase 0 is locked — the things that must survive every change>

## Non-goals

<never build these; architectural exclusions, not config toggles>

## Conventions

<test command, lint command, how to run, layout rules>
